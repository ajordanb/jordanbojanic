[project]
name = "portfolio-tools"
version = "0.1.0"
description = "Scripts and API client for the portfolio backend"
requires-python = ">=3.11"
dependencies = [
    "httpx>=0.27",
    "pydantic-settings>=2.0",
    "loguru>=0.7",
    "motor>=3.6",
]
